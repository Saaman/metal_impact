// we could highlight the checkbox to show it's been clicked:
$('select#user_role').removeClass("ajax-success ajax-error")
$("form#edit_user_<%= @user.id %> > select#user_role").addClass("ajax-success")