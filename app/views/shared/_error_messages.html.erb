<% if object.errors.any? %>
  
  <div class="alert alert-warning" style="float: left; margin-left: 20px;">
    <h3><%= t ".errors_main", error_count: t(".errors_count", count: object.errors.count), entity: t(".#{object.class.to_s.underscore}") %></h3>
    <p><%= t ".errors_help" %></p>
  </div>
  <div style="clear: left;"></div>

  <% if user_is_admin? %>
  	<ul>
  	<% object.errors.full_messages.each do |msg| %>
  		<li><%= msg %></li>
  	<% end %>
  	</ul>
  <% end %>

<% end %>