<h1>Listing artistes</h1>

Note : les artistes modifiés récemment sont en haut de la liste
<br /><br />
<table>
  <tr>
    <th>Nom</th>
    <th>Pays</th>
    <th>Pratiques</th>
    <th></th>
    <th></th>
  </tr>

<% @artists.each do |artist| %>
  <tr>
    <td><%= artist.name %></td>
    <td><%= artist.get_countries_string %></td>
    <td><%= artist.get_practices_string %></td>
    <td><%= link_to 'Show', artist %></td>
    <td><%= link_to 'Edit', '#' %></td>
  </tr>
<% end %>
</table>

<br />

<%= will_paginate @artists %>

<br />

<%= link_to 'Nouvel artiste', '#' %>
