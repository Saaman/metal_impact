<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :defaults => { :label_html => { :class => 'span3' }, :placeholder => false}, remote: true) do |f| %>
	
	<%= render 'form', f: f %>
	<div>
		<%= recaptcha_tags %>
		<%= render "links" %>
		<div class="btn-toolbar">
			<%= f.button :submit, :class => 'btn-primary' %>
    </div>
	</div>
<% end %>