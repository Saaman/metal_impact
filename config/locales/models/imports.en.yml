en:
  activerecord:
    models:
      import/metal_impact_entry: Metal Impact entry
    enums:
      import:
        source_file:
          source_type:
            metal_impact:: Metal Impact
        entry:
          target_model:
            user: User
    attributes:
      import/source_file:
        source_type: "Import type"
      import/entry:
        source_id: "Source id"
        target_model: "Entity type"
        target_id: "New id"
        data: "Data block"
    states:
      import:
        source_file:
          new: New
          loaded: Loaded
  simple_form:
    labels:
      import_entry:
        data: "Data block"
  errors:
    import_entry:
      default: "An error occurred when saving the entry"